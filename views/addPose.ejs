<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Add Pose</title>
    <link rel="stylesheet" href="./style.css">
    <link href="https://fonts.cdnfonts.com/css/hansen-shadow" rel="stylesheet">
    <link href="https://fonts.cdnfonts.com/css/jelly-anika" rel="stylesheet">
    <link rel="icon" href="./favicon.ico" type="image/x-icon"> 
  </head>
  <body>
    <main>
        <h1>Add pose</h1>  
        <form action = "/inputPose" method="POST" enctype="multipart/form-data">
            <label for="poseTitle">Title</label>
            <input type="text" id="poseTitle" name="title">

            <select id="selectNumber" name="difficulty_level">
              <option>Choose difficulty</option>
              </select>
  
          <script>
            var select = document.getElementById("selectNumber");
            var options = ["Advanced", "Intermediate", "Begginer"];
  
            for(var i = 0; i < options.length; i++) {
                var opt = options[i];
                var el = document.createElement("option");
                el.textContent = opt;
                el.value = opt;
                select.appendChild(el);
              }
          </script>

            <label for="poseImg">image</label>
            <input type="file" accept="image/png, image/jpg, image/jpeg" id="PoseImg" name="image">

            <label for="desc">description</label>
            <textarea id="desc" name="description"></textarea>

            <button type="submit" onclick="return confirm('Are you sure you want to add this pose?');" onMouseOver="mouseOver()" onMouseOut="mouseOut()" id="add">Add Pose</button>
        </form>
    </main>
  </body>
</html>

<script>
  function mouseOver() {
    document.getElementById("add").style.color = "#CC0099";
    document.getElementById("add").style.background = "#F7DFF1";
  }
  
  function mouseOut() {
    document.getElementById("add").style.color = '#C866B0';
    document.getElementById("add").style.background = "#FFF7FD";
  }
  </script>